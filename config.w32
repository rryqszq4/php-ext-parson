// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("parson", "for parson support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("parson", "enable parson support", "no");

if (PHP_PARSON != "no") {
	EXTENSION("parson", "parson.c");
}

